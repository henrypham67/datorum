# version_settings() enforces a minimum Tilt version
# https://docs.tilt.dev/api.html#api.version_settings
version_settings(constraint='>=0.33.3')

print('Hello Tiltfile')

k8s_yaml(['app/k8s/local/db.yaml'])

print('go go go')